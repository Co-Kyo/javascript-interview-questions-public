# 解题讲解：对象路径取值 get(obj, path)

## 第一步：理解问题本质

`get` 函数的核心是**按路径逐层访问嵌套属性**，但要保证安全——任意一层不存在时不抛错，而是返回默认值。

这是前端最常见的防御性编程场景：API 返回的数据结构不确定，不能直接写 `res.data.user.name` 这种链式访问。

## 第二步：路径解析——把字符串变成 key 数组

路径有两种写法，需要统一处理：

| 路径字符串 | 解析结果 |
|-----------|---------|
| `'a.b.c'` | `['a', 'b', 'c']` |
| `'a[0].b'` | `['a', '0', 'b']` |
| `'a.b[0].c.d'` | `['a', 'b', '0', 'c', 'd']` |
| `"a['some.key']"` | `['a', 'some.key']` |

**核心正则**：`/\[(\d+)\]|\[(['"])(.*?)\2\]|([^.\[\]]+)/g`

- `\[` 匹配方括号内的数字索引：`[0]`, `[123]`
- `\[` 匹配方括号内的引号字符串 key：`['key']`, `["key"]`（引号被自动剥离）
- `[^.\[\]]+` 匹配点号分隔的字段名

**为什么用 `match` 而不是 `split`？**

`split(/./)` 会在**所有**点号处切割，包括引号内的点号。例如 `a['some.key'].b` 会被错误切成 `["a", "'some", "key'", "b"]`。`match` 方式逐 token 提取，天然不会被引号内的分隔符干扰。

同时支持 `path` 直接传数组的情况（Lodash 也支持），此时跳过解析直接使用。

## 第三步：逐层取值——遍历 key 数组

```
current = obj
for key in keys:
    if current == null:  ← 关键！null 和 undefined 都要拦截
        return defaultValue
    current = current[key]
```

**为什么用 `== null` 而不是 `=== undefined`？**

因为 `null` 也没有属性，访问 `null.xxx` 同样会报错。用 `== null` 同时覆盖 `null` 和 `undefined`。

## 第四步：处理边界情况

| 场景 | 处理方式 |
|------|---------|
| 路径为空字符串 `''` | `parsePath` 返回空数组，循环不执行，直接返回 `obj` 本身 |
| 值为 `0`、`false`、`null` | 这些是**有效值**，只有 `undefined` 才用 `defaultValue` 替换 |
| path 为数组 | 跳过字符串解析，直接当 key 数组用 |
| 属性名含引号 | `match` 方式支持 `'key'` 和 `"key"` 两种写法，引号自动剥离 |
| 引号内含点号 | `match` 方式不会在引号内的 `.` 处分割，如 `a['some.key']` → `['a', 'some.key']` |

**特别注意**：最终判断是 `current === undefined`，而不是 `!current`。否则 `get({a: 0}, 'a', 'default')` 会错误返回 `'default'`。

## 第五步：复杂度分析与延伸

- **时间复杂度**：O(n)，n 为路径深度，每个 key 只访问一次
- **空间复杂度**：O(m)，m 为路径段数，用于存储解析后的数组

**面试延伸问题**：

1. 如何实现对应的 `set(obj, path, value)`？
2. 如果路径中包含转义的点号（如 `'a\.b'` 表示 key 本身就是 `a.b`）怎么处理？
3. 如何支持通配符路径，如 `'a[*].b'` 取所有数组元素的 b 属性？
4. 与 Optional Chaining `?.` 相比，`get` 的优势在哪？（动态路径、默认值、数组形式路径）
